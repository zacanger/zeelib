// @flow

/**
 * Get the arity of f
 * @example
 * const addFour = (a, b, c, d) => a + b + c + d
 * arity(addFour) // => 4
 */

const arity = (f: (any) => any): number =>
  f.length

export default arity
