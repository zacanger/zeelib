// @flow

/**
 * See if an object has a property
 * @example
 * has('a' { b: 'c' }) // => false
 */

const has = (p: string, o: Object): bool =>
  Object.prototype.hasOwnProperty.call(o, p)

export default has
