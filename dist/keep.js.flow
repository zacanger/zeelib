import id from './id'
import filter from './filter'

/**
 * Returns an array with all falsey values removed
 * @param {Array} a
 * @returns {Array}
 * @example
 * keep([ 'a', null, '', 2]) // => [ 'a', 2 ]
 */

const keep = (a) =>
  filter(id, a)

export default keep
