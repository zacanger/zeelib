// @flow

/**
 * Returns true if first param is less than or equal to second param
 * @example
 * lte(2, 1) // => false
 * lte(1, 1) // => true
 */

const lte = (a: number, b: number): bool =>
  a <= b

export default lte
